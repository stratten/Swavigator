/** Dimensions for the compact (collapsed) panel state. */
export const COMPACT_WIDTH = 220;
export const COMPACT_HEIGHT = 36;

/** Default dimensions for the expanded vertical layout. */
export const DEFAULT_EXPANDED_WIDTH = 280;
export const DEFAULT_EXPANDED_HEIGHT = 400;

/** Default dimensions for the expanded horizontal layout. */
export const DEFAULT_HORIZONTAL_WIDTH = 800;
export const DEFAULT_HORIZONTAL_HEIGHT = 220;

/** Default dock mode trigger strip size in pixels. */
export const DEFAULT_DOCK_TRIGGER_SIZE = 8;

/** Default dock mode trigger strip opacity (nearly invisible). */
export const DEFAULT_DOCK_TRIGGER_OPACITY = 0.02;

/** Default delay (ms) before auto-hiding after cursor leaves. */
export const DEFAULT_DOCK_HIDE_DELAY = 800;
